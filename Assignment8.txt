Create the following Table.

    CREATE TABLE customer(
    cust_id NUMBER(4) PRIMARY KEY,
    cust_city VARCHAR2(15),
    product_name VARCHAR2(15),
    product_price NUMBER(7,2)NOT NULL,
    quantity NUMBER(5)NOT NULL,
    bill_amt NUMBER(10,2)NOT NULL
    );

1. Display Table Structure.

    DESC customer;

2. Insert 10 records.

    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (1,'botad','book',100,5,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (2,'kaniyad','ball',35.5,6,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (3,'hamapar','cpu',1500,6,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (4,'botad','handwash',5,25,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (5,'lathidad','dvd',2500,3,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (6,'vichhiya','bag',750,4,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (7,'targhara','phone',15800,3,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (8,'botad','mouse',10.5,7,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (9,'paliyad','keyboard',150,6,0);
    INSERT INTO customer (cust_id,cust_city,product_name,product_price,quantity,bill_amt) VALUES (10,'bodi','cup',35,15,0);

3. Insert “Amreli” as a city for rccord 1, 3, 5 and “Vadodara” as a city for record 2, 4, 6, 8. And Surat for all other records.

   // Switch case
   UPDATE customer SET cust_city = 
    CASE
    WHEN cust_id IN (1, 3, 5) THEN 'Amreli'
    WHEN cust_id IN (2, 4, 6, 8) THEN 'Vadodara'
    ELSE 'Surat'
    END;


4. Update city='Mumbai” where city=“Surat”.

    UPDATE customer SET cust_city = 'Mumbai' WHERE cust_city = 'Surat;

5. Add New Column “Discount' With Appropriatc Data type.

    ALTER TABLE customer ADD discount NUMBER(7,2);

6. Calculate'“Bill Amt". (Product Prise * Ouantity)

    UPDATE customer SET bill_amt = product_price * quantity;
    
   // ALTER TABLE customer MODIFY bill_amt NUMBER(10,2);

7. Find Discount 10% if Bill_ Amt amount is greater than 10000.

    UPDATE customer SET discount = (bill_amt * 10) / 100 WHERE bill_amt > 10000;

8. Fetch all records that are not eligible for discount.

    SELECT *FROM customer WHERE bill_amt < 10000 ;

9. Delete AlL Records whosc Bill Amt amount is less than 1000.

    DELETE FROM customer WHERE bill_amt < 1000;

10. Display all bills whose bill amount is between Rs. 2000 to Rs. 5000.

    SELECT *FROM customer WHERE bill_amt BETWEEN 2000 AND 5000;